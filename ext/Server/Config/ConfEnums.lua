DataTypes = {
    str = "str",
    bool = "bool",
    short = "short",
    int = "int",
    long = "long",
    float = "float"
}

Flag = {
    lockDevBuildTrue = "lockDevBuildTrue", -- Lock value on true when development build is enabled in the registry.
}

Container = {
    checkbox = "checkbox"
}